import { Typography } from '@mui/material';

export default function CheckoutPage() {
  <Typography variant="h3">Only logged in user can use this page</Typography>;
  return <h1>Check out Page</h1>;
}
